import { createSlice } from "@reduxjs/toolkit";
initialState = []

createSlice({
    name:"todo",
    initialState,
    reducers:{
        addItems(state,action){
            ret
        }
    }
})
