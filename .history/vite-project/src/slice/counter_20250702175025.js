import { createSlice } from "@reduxjs/toolkit";

initialState = 0

createSlice({
    name:'counter',
    initialState ,
    reducers:{
        increment(state,action){
            return state+=1

        }
        increment(state,action){
            return state+=1

        }
    }
})