import React from 'react'

const ToDoFilter = ({setFilter}) => {
  return (
    <div>
    <h3>
        Filter
    </h3>
    <button onClick={} value={"all"} >All</button>
    <button onClick={} value={"active"} >Active</button>
    <button onClick={} value={"completed"} >Completed</button>
      
    </div>
  )
}

export default ToDoFilter
