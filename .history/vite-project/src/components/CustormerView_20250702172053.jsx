import { useSelector } from "react-redux"

export default function CustomerView(){
    useSelector((state))
    return <div>
        <h3>View customer </h3>
        
            {/* <ul style={{listStyle:'none'}}>
                {custormer.map((e,i)=>
                     <li key={i}>
                        {e}

                </li>
                )}
               
            </ul> */}
      
    </div>
}